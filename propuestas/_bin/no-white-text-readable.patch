diff --git a/propuestas/academia/index.html b/propuestas/academia/index.html
index 43a76ee..4cef4b8 100644
--- a/propuestas/academia/index.html
+++ b/propuestas/academia/index.html
@@ -313,12 +313,12 @@ figure { margin: 0; }
 .hero__lede,
 .section__lede {
   font-size: 1.15rem;
-  color: var(--muted);
+  color: var(--ink);
   max-width: 38rem;
   margin: 0.85rem auto 0;
   font-style: italic;
 }
-.hero__lede { font-size: 1.05rem; }
+.hero__lede { font-size: 1.05rem; color: var(--navy); }
 .hero__lede em { font-style: italic; color: var(--navy); }
 .hero__actions {
   display: flex;
@@ -643,7 +643,7 @@ figure { margin: 0; }
   font-size: 0.58rem;
   letter-spacing: 0.16em;
   text-transform: uppercase;
-  color: var(--muted);
+  color: var(--navy);
   padding: 0.5rem 0.2rem 0.55rem;
   text-align: center;
 }
diff --git a/propuestas/editorial/index.html b/propuestas/editorial/index.html
index d8741ba..d7c4a03 100644
--- a/propuestas/editorial/index.html
+++ b/propuestas/editorial/index.html
@@ -296,7 +296,7 @@ p:last-child { margin-bottom: 0; }
   margin-top: 1.25rem;
   max-width: 38rem;
   font-size: 1.12rem;
-  color: var(--muted);
+  color: var(--ink);
   font-weight: 400;
 }
 
@@ -324,8 +324,12 @@ p:last-child { margin-bottom: 0; }
   margin: 0 auto 1.1rem;
   font-size: 0.78rem;
   font-style: italic;
-  color: var(--muted);
+  color: var(--ink);
   letter-spacing: 0.01em;
+  display: inline-block;
+  padding: 0.35rem 0.65rem;
+  background: rgba(250, 247, 241, 0.92);
+  border: 1px solid var(--rule);
 }
 
 /* ——— Stats ——— */
diff --git a/propuestas/sellos/index.html b/propuestas/sellos/index.html
index 213edbd..36d23e4 100644
--- a/propuestas/sellos/index.html
+++ b/propuestas/sellos/index.html
@@ -302,12 +302,12 @@ textarea {
 }
 .btn--ghost {
   background: transparent;
-  color: white;
-  border-color: rgba(255, 255, 255, 0.6);
+  color: var(--color-navy);
+  border-color: color-mix(in oklab, var(--color-navy) 55%, transparent);
 }
 .btn--ghost:hover {
-  background: rgba(255, 255, 255, 0.12);
-  color: white;
+  background: color-mix(in oklab, var(--color-navy) 8%, transparent);
+  color: var(--color-navy);
 }
 .hero__cta .btn--ghost {
   color: var(--color-navy);
@@ -380,7 +380,7 @@ textarea {
 }
 .brand__tag {
   font-size: 0.72rem;
-  color: var(--color-text-muted);
+  color: var(--color-navy);
   letter-spacing: 0.06em;
   text-transform: uppercase;
 }
@@ -512,12 +512,13 @@ textarea {
     linear-gradient(180deg, rgba(255,250,245,0.14) 0%, rgba(255,250,245,0.03) 40%, rgba(255,250,245,0.72) 100%);
 }
 .hero__panel {
-  background: transparent;
-  border: none;
-  border-radius: 0;
-  padding: 0;
+  /* Soft cream scrim so navy copy stays readable on the campus photo */
+  background: rgba(255, 250, 245, 0.82);
+  border: 1px solid rgba(236, 214, 192, 0.55);
+  border-radius: 1.1rem;
+  padding: var(--space-6) var(--space-6) var(--space-5);
   max-width: 640px;
-  box-shadow: none;
+  box-shadow: 0 18px 40px -18px rgba(11, 42, 82, 0.22);
 }
 .hero__content {
   padding-block: var(--space-12) var(--space-14);
@@ -575,7 +576,7 @@ textarea {
 }
 .hero__lede {
   font-size: clamp(1rem, 2.4vw, 1.125rem);
-  color: var(--color-text-muted);
+  color: var(--color-navy);
   max-width: 60ch;
   margin-bottom: var(--space-7);
   line-height: 1.55;
@@ -617,7 +618,7 @@ textarea {
 }
 .hero__meta span {
   font-size: 0.72rem;
-  color: var(--color-text-muted);
+  color: var(--color-navy);
   text-transform: uppercase;
   letter-spacing: 0.08em;
   line-height: 1.3;
@@ -666,7 +667,7 @@ textarea {
 }
 .section__lede {
   font-size: var(--text-lg);
-  color: var(--color-text-muted);
+  color: var(--color-text);
   max-width: 60ch;
 }
 .section--dark .section__lede {
@@ -1214,8 +1215,9 @@ img[data-k="campus-cancha-cubierta"] {
   flex-direction: column;
   gap: var(--space-2);
   padding: var(--space-6) var(--space-5) var(--space-5);
-  background: linear-gradient(180deg, transparent 0%, rgba(0, 0, 0, 0.65) 60%, rgba(0, 0, 0, 0.85) 100%);
-  color: white;
+  /* Soft cream scrim — navy letters stay readable on the photo */
+  background: linear-gradient(180deg, rgba(255, 250, 245, 0) 0%, rgba(255, 250, 245, 0.88) 42%, rgba(255, 250, 245, 0.96) 100%);
+  color: var(--color-navy);
 }
 .vida__hero-quote {
   font-family: var(--font-display);
@@ -1223,6 +1225,7 @@ img[data-k="campus-cancha-cubierta"] {
   line-height: 1.35;
   font-style: italic;
   max-width: 46ch;
+  color: var(--color-navy);
 }
 .vida__hero-tag {
   font-size: 0.7rem;
@@ -1383,7 +1386,7 @@ img[data-k="campus-cancha-cubierta"] {
 }
 .level__building span {
   font-size: 0.72rem;
-  color: var(--color-text-muted);
+  color: var(--color-navy);
   letter-spacing: 0.04em;
 }
 
@@ -1453,7 +1456,7 @@ img[data-k="campus-cancha-cubierta"] {
 .credential a { color: var(--color-primary); }
 .credential--toefl { border-color: var(--color-navy); }
 
-/* ─── Intro type over Chiba stickers (cream halo + faint local wash, no white card) ─── */
+/* ─── Soft cream scrim only — no white letter-halos on paper/photo ─── */
 .section:not(.section--dark) .section__title,
 .section:not(.section--dark) .section__lede,
 .section:not(.section--dark) .section__eyebrow,
@@ -1463,14 +1466,21 @@ img[data-k="campus-cancha-cubierta"] {
 .contact__copy > p,
 .contact__list dt,
 .contact__list dd {
-  text-shadow:
-    0 0 0.04em #fff8f0,
-    0 0 0.08em #fff8f0,
-    0 0 0.12em #fffaf5,
-    0.03em 0.02em 0.08em rgba(255, 248, 240, 0.95),
-    -0.03em 0.02em 0.08em rgba(255, 248, 240, 0.95),
-    0.03em -0.02em 0.08em rgba(255, 248, 240, 0.95),
-    -0.03em -0.02em 0.08em rgba(255, 248, 240, 0.95);
+  text-shadow: none;
+  color: var(--color-navy);
+}
+.section:not(.section--dark) .section__lede,
+.mv__intro,
+.mv__block p,
+.contact__copy > p,
+.contact__list dd {
+  color: var(--color-text);
+}
+.section:not(.section--dark) .section__eyebrow {
+  color: var(--color-primary);
+}
+.mv__block h3 {
+  color: var(--color-primary);
 }
